version: '3.8'
services:
  app:
    image: node:18-alpine
    container_name: edutrack-jenkins
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3001:3000"
    command: sh -c "npm install && node server.js"
    environment:
      - PORT=3000
volumes:
  db-data:
